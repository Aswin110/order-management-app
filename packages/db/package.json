{
  "name": "@order-operations/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev",
    "push": "prisma db push",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"db package: lint via dependents\" && exit 0",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@prisma/client": "^6.16.3"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "dotenv": "^16.6.1",
    "prisma": "^6.16.3",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}